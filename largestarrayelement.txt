#include <stdio.h>
int main()
{
    int a[5],i,max;
    for(i=0;i<5;i++)
    {
        printf("enter elements of array:\n");
        scanf("%d", &a[i]);
    }
    max=a[0];
   for(i=0;i<5;i++)
   {
      if (a[0]<a[i])
      {
          a[0]=a[i];
          max=a[i];
      }
      
   }
   printf("largest element of the array is %d",max);
  
   return 0;
}

