#include <stdio.h>
#include<math.h>
int main()
{
    char stra[100],strb[100];
    int i=0,j;
    printf("enter string:");
    scanf("%s",stra);
    while(stra[i]!='\0')
    {
        strb[i]=stra[i];
        i=i+1;
    }
    printf("copied string:%s",strb);
    return 0;
}

