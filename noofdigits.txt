#include <stdio.h>
#include<math.h>
int main()
{
    int a,count=0;
    printf("enter the number:");
    scanf("%d",&a);
    if(a==0)
    {
        count=1;
    }
    while(a!=0)
    {
        a=a/10;
        count=count+1;
    }
    printf("number of digits=%d",count);
}

