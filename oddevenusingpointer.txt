#include <stdio.h>

int main()
{
    int n, *p;

    printf("Enter number: ");
    scanf("%d", &n);

    p = &n;   // pointer stores address of n

    if(*p % 2 == 0)
        printf("Even number");
    else
        printf("Odd number");

    return 0;
}